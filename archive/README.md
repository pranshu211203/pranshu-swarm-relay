# Archive

Earlier iterations of the mission script, kept for history. These are **not** the
competition-final versions — see `mission/` for those.

They are here to show how the drone logic evolved (movement handling, UWB correction,
synchronisation) across the project rather than to be run.

- `Known_uwb_pranshu_6_OLDER.py` — earlier draft
- `Known_uwb_pranshu_6_OLD.py` — later draft, superseded by the `mission/` versions
